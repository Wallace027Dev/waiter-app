# Connection string do MongoDB (Atlas).
# IMPORTANTE: se esta credencial já foi commitada antes, rotacione a senha no Atlas.
MONGO_URL=mongodb+srv://usuario:senha@cluster.mongodb.net/waiterapp

# Porta do servidor (opcional, padrão 3001)
PORT=3001
